resource "aws_key_pair" "!!REGION_ALIAS!!_admin_access" {
  key_name   = "admin_access"
  public_key = tls_private_key.global_ssh_key.public_key_openssh
  provider = aws.!!REGION_ALIAS!!

  lifecycle {
      ignore_changes = [ public_key ]
  }
}
